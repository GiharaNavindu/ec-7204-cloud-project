spring:
  datasource:
    url: jdbc:h2:mem:userservicedb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

app:
  jwt:
    secret: test_secret_for_user_service_jwt_needs_to_be_at_least_sixty_four_chars_long_12345
    expiration: 3600000
